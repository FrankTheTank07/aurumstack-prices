# AurumStack Website

Static GitHub Pages website for AurumStack.

## Preview locally

From this folder, run:

```bash
python3 -m http.server 4173
```

Then open `http://localhost:4173`.

## Publish with GitHub Pages

Push the contents of this folder to a GitHub repository, then enable GitHub Pages for the repository's `main` branch from the root folder.

